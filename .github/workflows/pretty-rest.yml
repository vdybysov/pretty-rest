name: pretty-rest

on:
  push:
    branches: [master]

jobs:
  init:
    name: Checkout and setup node
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 12.x
        uses: actions/setup-node@v1
        with:
          node-version: 12.x
